You have to install: 
npm install serialport --save
npm install express --save
npm install socket.io --save

Connect the arduino to a pc (raspberry). 
Maybe change the seriel port ("/dev/ttyACM0"). 
Start the server by typing "node index.js" in the terminal in the directory of te index.js script.
